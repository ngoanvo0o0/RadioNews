// Tobe defined, because we dont know what is it and how is it work.
class About {
  String id;
  int followers;
  int listeners;
  String firstUrl;
  String secondUrl;

  About(this.id, this.followers, this.listeners, this.firstUrl, this.secondUrl);
}
