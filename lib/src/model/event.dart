// Tobe defined, because we dont know what is it and how is it work.
class Event {
  String id;
  String type;
  String title;
  DateTime releaseDate;
  String chanelUrl;

  Event(this.id, this.type, this.title, this.releaseDate, this.chanelUrl);
}
