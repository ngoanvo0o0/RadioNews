import 'package:flutter/material.dart';

class AppBarDimension {
  const AppBarDimension._();

  static const logo = 64.0;
  static const search = 20.0;
  static const toolBar = 80.0;
}

class RecordingListDimension {
  const RecordingListDimension._();

  static const line = 2.0;
  static const header = 0.10; // 10% viewport.
  static const recording = 0.40; // 40% viewport.
  static const carousel = 0.40; //40% viewport.
  static const footer = 0.1; // 10% viewport
}

class HeaderDimension {
  const HeaderDimension._();

  static const earth = 20.0;
}

class EventDimension {
  const EventDimension._();

  static const eventIcon = 0.15; //15% viewport
}

class AboutDimention {
  const AboutDimention._();
  static const earth = 20.0;
}

// class AppDimension{
//   //default size, match with Iphone 14 ProMax
//   final Size DeviceSize = const Size(430, 932);
//   AppDimension(Size DeviceSize);

  

// }